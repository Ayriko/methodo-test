package td.library.domain.port

import td.library.domain.model.Book

class BookRepositoryPort {
    fun getAllBooks(): List<Book> {
        return TODO("Provide the return value")
    }

    fun addBook(book: Book): Book {
        return TODO("Provide the return value")
    }
}